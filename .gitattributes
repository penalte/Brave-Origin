# Normalize all text files to LF in the working tree and repository.
# This is a Linux/Docker project; CRLF churn from external sync tools breaks diffs and shell scripts.
* text=auto eol=lf

# Unified diff context intentionally prefixes blank lines and tab indents with a space.
*.patch whitespace=-blank-at-eol,-space-before-tab
