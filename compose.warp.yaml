# Required with compose.yaml for private OIDC routing, or legacy WARP routing.
services:
  brave-origin:
    cap_add:
      - NET_ADMIN
    volumes:
      - warp-state:/var/lib/cloudflare-warp
volumes:
  warp-state:
